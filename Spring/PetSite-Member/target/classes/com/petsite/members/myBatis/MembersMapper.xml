<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.petsite.members.dao.MemberDao">

	<!-- 회원가입 -->
	<insert id="insertMem"
	parameterType="com.petsite.members.domain.MemberInfo">
	insert into members (m_id, m_name, m_pw, m_address, m_date, m_img) 
	values (#{id},#{name},#{pw},#{address},#{regDate},#{pic_name})
	</insert>
</mapper>